{
  "serena": {
    "type": "stdio",
    "command": "uv",
    "args": [
      "run",
      "--directory",
      "${SERENA_PATH}",
      "serena",
      "start-mcp-server",
      "--context",
      "claude-code",
      "--project",
      "${PROJECT_ROOT}"
    ],
    "env": {},
    "_comment": "Serena MCP v1.3.0+ — start-mcp-server サブコマンド + --context claude-code（旧 serena-mcp-server は廃止）。alwaysLoad は付けない: tool-search deferred で schema 約 10KB の毎 session 先頭 load を避ける (2026-07-16 転換、README 参照)"
  }
}
