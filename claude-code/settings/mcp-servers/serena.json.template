{
  "serena": {
    "type": "stdio",
    "command": "uv",
    "args": [
      "run",
      "--directory",
      "${SERENA_PATH}",
      "serena-mcp-server",
      "--project",
      "${PROJECT_ROOT}"
    ],
    "env": {},
    "alwaysLoad": true,
    "_comment": "Serena MCP - Symbolic code analysis and project memory management. alwaysLoad: tool-search deferral skip (頻繁利用、2.1.121+)"
  }
}
