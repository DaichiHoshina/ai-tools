# 品質保証(Quality Assurance)

> **目的**: 間違いを防止し、成果物の品質を保証

---

## 4原則

| 原則 | 内容 |
|------|------|
| ゼロ欠陥 | 段階的検証・自己レビュー・自動チェック |
| 多層防御 | 作業中→完了時→自動ツール→最終確認（各層 ≥ 90%検出率） |
| 早期検出 | 作業中=1x コスト、作業後=10x、納品後=100x |
| 継続的改善 | 失敗→原因分析→プロセス改善→再発防止 |

---

## 作業中チェック

### 自己質問リスト

```
□ 今やっていることは正しいか?
□ 見落としはないか?
□ エッジケースを考慮したか?
□ テストは通るか?
□ 型安全性は保たれているか?
□ セキュリティは大丈夫か?
□ パフォーマンスへの影響は?
□ 既存コードを壊していないか?
```

---

## 作業完了時チェックリスト

### コード品質

```
□ 型安全性: any/Any/interface{} を不使用、全型明示
□ テストカバレッジ ≥ 80%、全エッジケースをカバー
□ 関数サイズ ≤ 50行 / 循環的複雑度 ≤ 10 / 引数 ≤ 3個
□ 意図が明確な命名 / マジックナンバー不使用
```

### 機能要件

```
□ 仕様通りに実装 / 全要件を満たす
□ エッジケース考慮 / エラーハンドリング適切
```

### 非機能要件

```
□ パフォーマンス: 応答時間 < 200ms(API) / N+1問題なし / メモリリークなし
□ セキュリティ: SQL Injection / XSS / CSRF対策 / 認証・認可実装
□ 可用性: エラー回復 / ログ出力 / モニタリング可能
```

### ドキュメント

```
□ README更新 / API仕様書更新 / コメント追加
```

---

## 自動検証

### 静的解析コマンド

```bash
# TypeScript
npm run type-check && npm run lint

# Go
go vet ./... && golangci-lint run

# Python
mypy src/ && pylint src/
```

### セキュリティスキャン

```bash
npm audit / pip-audit / go list -json -m all | nancy sleuth
git secrets --scan
```

---

## レビュープロトコル

### セルフレビューチェックリスト

```
コード:
□ ロジック正確 / エッジケース考慮 / エラーハンドリング適切
□ 可読性良好 / 重複コードなし

テスト:
□ 正常系 / 異常系 / 境界値テストあり / 全テストPass

セキュリティ:
□ 入力検証 / SQL Injection対策 / XSS対策 / 認証・認可チェック済み

パフォーマンス:
□ N+1問題なし / 不要ループなし / メモリリークなし
```

### ペアレビューフロー

```
Developer Agent → QA Agent → 問題あれば差し戻し → 問題なければ承認
```

---

## エラーパターン・アンチパターン

### 検出すべきパターン

| パターン | 重大度 | 対処 |
|---------|--------|------|
| `: any` 型使用 | 高 | 具体的な型を指定 |
| `== null`（==使用） | 中 | `===` を使用 |
| DOM への未サニタイズHTML挿入 | 高 | textContent を使用 or HTMLサニタイザー |
| SQLクエリへの変数直接展開 | 高 | プレースホルダーを使用 |

### アンチパターン

| 名前 | 検出条件 | 解決策 |
|------|---------|--------|
| God Object | クラスサイズ > 500行 | クラスを分割 |
| Deep Nesting | ネスト深度 > 4 | Early return / Guard Clause |
| Magic Number | リテラル数値が直接使用 | 定数として定義 |

---

## 品質基準

| 指標 | 基準値 |
|------|--------|
| 型安全性 | 100% |
| テストカバレッジ | ≥ 80% |
| 循環的複雑度 | ≤ 10 |
| コード重複率 | ≤ 5% |
| バグ数 | 0 |
| 脆弱性数 | 0 |
| 応答時間 | ≤ 200ms |
| セキュリティスコア | ≥ 90 |

---

## 事後分析(Post-Mortem)フォーマット

```markdown
### 根本原因(5 Whys)
1. なぜエラーが発生したか?
2. なぜその状態になったか?
3. なぜ防げなかったか?
4. なぜチェックで見逃したか?
5. なぜプロセスが不十分だったか?

### 再発防止策
- 即座の対策 / 短期的対策 / 長期的対策
```

---

## 最終確認チェックリスト

```
□ セルフレビュー完了
□ 全テストPass / カバレッジ ≥ 80%
□ 型安全性100%
□ セキュリティチェック完了
□ パフォーマンス基準達成
□ ドキュメント更新
□ エラーパターン未検出
□ アンチパターン未検出
□ 品質ゲートPass

全てチェックされるまで作業完了としない
```

---

**多層防御、継続的検証、失敗からの学習で高品質を保証**
