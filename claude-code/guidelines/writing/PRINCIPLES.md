# 文章執筆の共通原則

ヒト向けドキュメント共通原則。個別ルールは各 file ([commit-message](commit-message.md) / [pr-description](pr-description.md) / [external-post](external-post.md) / [long-form-doc](long-form-doc.md) / [design-doc-protocol](design-doc-protocol.md)) 参照。

本 file は約 500 行あるため、用途に応じて読む section を絞る (全文 load は深い書き直し時のみ)。

| 用途 | 読む section |
|---|---|
| check / rewrite 実行 (jp-writing) | 最優先 / 初読者基準 / 書く前の4問 / 守る指針 / 媒体別構造 / ミクロ規則 / 文単位の品質規約 / 出力前セルフチェック / 機械検出 grep |
| 外向き text の書込前 check (writing-check-protocol) | ミクロ規則 / Web 可読性 |
| web 向け文書の執筆 | 媒体別構造 / Web 可読性 / PREP 法 + 5W1H |
| 深い書き直し・AI 臭の除去 | 全文 (AI臭を消す3変換 / 避けるパターン含む) |

## 最優先 — 読み手の認知負荷を下げる

ドキュメントは記憶ダンプではなく、判断や行動を助ける道具として機能する。すべてのルールはこの目的から派生する。

- 1回読めば理解できる文章を目指す
- 読み手に「で、つまり何？」と思わせない
- 「賢そう」でなく「伝わる」を優先する
- 初見の人が途中で止まらないこと

迷ったらこの4つに戻る。判断の優先順は「ルール準拠」より「読み手の負荷」。

### 専門語・翻訳調・カタい語を使わない (読み手知らない語禁止)

書き手が知ってる語 ≠ 読み手が知ってる語。中学生でも分かる日常和語を選ぶ。迷ったら短い和語。

- × 外挿 / 余裕大 / 無視可 / 無視可能 / 懸念解消 / 全観点 / 判定確定 / 漸近 / 線形 / 冪等性 / 直交 / ロバスト
- ○ 本番だと / 枠の半分以下 / 気にしなくていい / 心配なし / 全部OK / GOで進める

英語のまま残すのは code 識別子 / command 名 / 固有名詞 / 定着済み開発用語 (commit / PR / hook / lint / API 等) に限る (基準は `rules/plain-jp.md` と共通)。日本語で自然に言える一般語は日本語で書き、置換は [PRINCIPLES-word-replace.md](PRINCIPLES-word-replace.md) に従う。**書き手の語彙でなく読み手の語彙で書く**。本 section は外向き text だけでなく chat 応答にも適用する。

書く前の self-check: 「この語、user が知らないと言ったら言い換えられるか？」答え No なら使わない。

## 初読者基準 — 初めて読む人でも分かる文章

読み手は「この会話・この repo・この PR を初めて見る人」と想定して書く。書き手が経過で得た文脈 (略称・固有名詞・前提知識) は読み手の頭にない。本節は分散していた基準の統合先で、chat 応答にも外向き text にも適用する。

### 語彙・文長の基準選択表 (どれをいつ使うか)

| 出力先 | 語彙基準 | 1 文上限 | 詳細 |
|---|---|---|---|
| chat 応答 / 技術 doc (PR / DD / RCA) | 中学生が辞書なしで読める和語 | 100 字 (短文 60 字) | 本 file 冒頭「専門語禁止」+ `rules/plain-jp.md` |
| web 向け文書 (blog / 社外公開) | 同上 | 60 字 | `## Web 可読性` |
| UI コピー / 通知文 | 小学 4 年生語彙 | 40 字 | `### UI コピー / エラーメッセージ向け追加規約` |

迷ったら厳しい側 (短く・平易に) を選ぶ。

### 用語は初出で開く (集約 pointer)

1. **難語・専門用語**: 削除でなく初出で 1 文の定義を併記する (`## AI臭を消す3変換` の (c)。例: 「idempotency (同リクエストを 2 回叩いても結果不変)」)
2. **内部 jargon**: 初出で和訳を添える (`### NG辞書` 直下の「内部jargon初出和訳必須」list)
3. **略語**: 初出でフル展開する (同「略語初出展開必須」list。例: RCA (Root Cause Analysis))
4. **固有名詞 (repo 名 / tool 名 / 社内システム名)**: 初出に 1 句の説明を添える (例: 「sync.sh (設定を `~/.claude/` へ同期する script)」)

### 前提知識を補う

- 会話の経過で作った略称・案番号 (「案A」「finding 3」「例の hook」) を最終出力に持ち込まない。中身を書き下す
- 「なぜこの話をしているか」が本文だけで分かるようにする。背景が 1 文で書けるなら冒頭に置く
- 読み手が本文を理解するのに別 file / 別スレッドを開く必要がある構成にしない。判断に要る要点は本文へ転記し、link は補助に使う

### 談話構造 — 段落は「主張 → 根拠 → 次の疑問への先回り」

段落を主張 1 文で始め、根拠を続け、読み手が次に聞きたくなる疑問に先回りして 1 文で答える。

- NG: 「cache 層を追加した。TTL は 60 秒。invalidation は event 駆動。」(名詞の羅列で、なぜ・どう効くかが読めない)
- OK: 「一覧 API に cache 層を追加して応答を p99 1.2s から 320ms に縮めた。TTL は 60 秒にした。在庫更新のずれが心配になるが、更新 event で即時に cache を消すので古い値は最大でも数秒しか残らない。」

## 書く前の4問

抽象的な答え不可。答えずに書くとAI文体になる。

1. **読み手は具体的に誰か** (×「エンジニア」○「新joinのsenior backend」「レビュアーAyanoさん」)
2. **読後に何を決めさせる / やらせるか** (×「理解させる」○「approveを決めさせる」「migrationを実行させる」)
3. **主張を裏付ける数字or事例** (×「大幅改善」○「p99 1.2s → 320ms (-73%)」)
4. **「なぜ？」と聞かれたら** (×「〜が必須」○「localStorageにtoken置かない、XSSで抜かれる」)

## chat 出力前 先手 sweep 4 pattern

chat / 外向き text の送信前に以下 4 pattern を目視で点検し、該当があれば送信前に直す。jp-quality-block hook の warn を retry で消すより、先手で消した方が token cost が安い。

1. **連続漢字≥5**: 5 文字以上の漢字連鎖 (例: 「行自動注入」「依頼準備中」「固有名詞系」) は助詞を挿入するか訓読みに開く。→ 「行の自動注入」「依頼を準備中」「固有名詞の系統」
2. **体言止め bullet**: bullet 内が名詞句で終わる (例: 「memory 逆流の掃除」) を「〜する / 〜した / 〜だ」で閉じる。→ 「memory 逆流を掃除した」
3. **100 字超文**: 1 文が 100 字を超えたら分割する (短文は 60 字目安)。読点でなく句点で切る
4. **矢印チェーン**: `A → B → C` の矢印羅列を、動詞を持つ文章に展開する。→ 「A を B に変換して C へ渡す」

先手 sweep は「書くと決めた文」に対する check で、書くかどうかの判定は先に済ませる (§書く前の 4 問)。詳細 grep は §機械検出 grep 参照。

## 守る指針 (8原則)

1. **用語を並べるだけで説明しない**: 専門用語の羅列は内容ゼロ。1用語ごとに「何のために・どう動くか」を1文添える
2. **暗黙知を前提にしない**: 「いつもの」「ご存知の通り」を使ったら、その内容を1文で書く
3. **次の疑問を先回り**: 1段落書いたら「読み手が次に何を聞きたいか」を想像し、次段落に答えを置く
4. **見出しだけで流れが分かる**: 見出しはラベル (「アーキテクチャ」) でなく主張 (「読み書きを分離して負荷を分散する」)
5. **段落に役割を持たせる**: 各段落は「背景 / 理由 / 具体例 / 結論 / 注意点」のどれかに該当。役割が曖昧なら削除or分割
6. **抽象説明の直後に具体例**: 抽象名詞や原則を書いたら、次の1-2文で具体例 (数値・事例・コード断片)
7. **「賢そう」より「伝わる」**: 平易で短い文を選ぶ。読み手は実務判断のために読む
8. **現在の事実のみを書く (経過メモ禁止)**: 「当初 X だったが Y に変更」「レビュー指摘で修正」等の開発中・執筆中の変遷を本文に残さない。変遷は git history / レビュースレッド / 版管理が保持する。不採用案が読み手の判断に必要な場合のみ「Y を採用した。X は〜のため不採用」と**現在形の採用理由**で 1-2 行書く。適用先: PR body / commit / issue / Slack / DD / RCA / code comment / chat 報告の全て

## chat 応答の識別子強調 (bold + backtick 併用)

chat 応答内で file path / 関数名 / 変数名 / shell command / key 名を初出や強調で示すときは、backtick 単独ではなく `**` + backtick 併用にする。

```
NG: `sync.sh` を実行する
OK: **`sync.sh`** を実行する
```

**Why**: chat の inline code (backtick 単独) は背景色が薄く、monospace だけでは地の文に埋もれる。

**How to apply**:

- chat 応答で識別子を初出 or 強調したいときは `**` + backtick で囲む
- 日本語の説明文や一般語には bold を付けない (noise になる)
- 外向き prose (PR / commit / doc) は対象外、従来通り backtick 単独で十分

## markdown 長文 doc の 1 文 1 行 + 子 bullet

DesignDoc / PRD / RCA など markdown 長文 doc は 1 文ごとに改行する。1 つの bullet を複数文に分けるときは、**2 文目以降を子 bullet (`- ` を付けて 1 段深く) にする**。素のインデント改行 (bullet なしで空白だけ下げる) は禁止。

### OK

```markdown
- サイズ行の UPDATE は、次の 2 条件を両方満たすときのみ許可する。
  - 対象商品が非アクティブ、かつ未発送 0 件。
  - それ以外は 409。
```

### NG

```markdown
- サイズ行の UPDATE は、次の 2 条件を両方満たすときのみ許可する。
  対象商品が非アクティブ、かつ未発送 0 件。
  それ以外は 409。
```

**Why**: 素のインデント改行は「これは何の継続?」を読み手が推測する必要がある。子 bullet 化で親子関係が視覚的に明確になる。

**How to apply**:

- 文を改行で割ったら、継続行の頭に `- ` を付けて 1 段ネストする
- 見出し直下の通常段落を複数文に割る場合も、`- ` bullet 化してから子 bullet にする
- 既存の非 bullet 複数行段落は diff を広げないため触らない。新規 / 編集した箇所だけ揃える

## ドラフト便宜名・過去経緯・自作造語の禁止

チーム共有文章 (DesignDoc / PR / issue / Slack) にドラフト中の便宜名 (「B-lite」「第 3 世代」「案 E」等) や自作造語を残さない。

- **NG**: 「B-lite」「第 3 世代」「案 E」など自作ラベル、「当初は案 B で検討していたが〜」の過去経緯
- **OK**: 「新方式」「旧方式」「事前シャッフル方式」など実装名 / 汎用用語で書く。比較は Alternatives 節に並列表で並べる

**How to apply**:

- 投稿・commit 前に本文を grep し、自作便宜名・「当初は」「以前は」を検出したら書き直す
- 難語・英語略語は初出で日本語補足を付ける (例: 「段階移行 (Parallel Change: 追加 → 移行 → 削除)」)
- 事実未確認の前提 (「既存に flag がある / ○○が存在」等) は `grep` / `find_symbol` / `gh pr view` で裏取りしてから書く

## 状態変化に追従して section 名を更新する

docs section は **現状を表す名前** にする。状態が変わったら section 名 / 表 column 名も更新する。

例 (架空):
- 試験前: 「未決事項」表 (status: in_progress 列あり)
- 試験完了後: 「検証結果」に rename、status 列削除 (完了で進捗 enum 不要)
- 仕様確定後: 「仕様」に rename、判断経緯は別 section に逃がす

**判断**: section 名が現状と乖離した瞬間に rename + cross-ref 更新。「未決事項」のまま中身が確定値の表になっていると読者が混乱する。

rename に伴い他 file / 同 file 内 cross-ref も `[未決事項 #X]` → `[検証結果 #X]` のように全置換する。

## 媒体別構造

### 短文 (issue / PRコメント / Slack / Notionコメント / ticket)

**PREP 3点構造**:

1. 結論: 読み手に何を判断 / 実行させるか (1行)
2. 理由: 現象 / 影響 / 原因
3. 次アクション: 担当 / 期限 / 不明点

- 本文400字前後 (30秒で読める)、title 80字前後
- 詳細ログ・スタックトレースは `<details>` 折りたたみor別ファイルリンク
- 詳細は [external-post.md](external-post.md) 参照

### 長文 (Design Doc / PRD / RCA / Notionページ)

- **TL;DR冒頭1-3文で結論**。「本稿では〜について述べる」系導入は削除
- **報告型doc (負荷試験 / 計測 / 障害 / 検証 / KPI共有) は冒頭3行で「最大値 / 測定条件 / 次アクション」必須**。最大値=計測した上限/閾値の具体数値、測定条件=どの環境/負荷/期間で測ったか、次アクション=読み手が approve / scale / 修正 のどれを決めるか。数値を結論行の本体に置き、別セクションに散らさない。
- 800-1500字 / ページ、2000字超なら分割
- 1セクション = 地の文3-5割 + 箇条書き / 表5-7割
- 箇条書きは3-5項目。7超は表or段落分割
- コードブロックは5行以内
- 詳細は [long-form-doc.md](long-form-doc.md) 参照

### PR description

テンプレ・記法は [pr-description.md](pr-description.md) を canonical とする。

### DesignDoc

12セクションor軽量5節。詳細は [design-doc-protocol.md](design-doc-protocol.md) 参照。

## PREP 法 + 5W1H (長文構造の規範)

長文出力は **PREP 法** で構成し、**why / how** を必須とする。**chat 内の作業完了報告 / 調査結果 / 検証結果 / RCA 概要 / 実装方針報告** にも同構造を適用する (10 行以上の報告は 4 ブロックを省略しない)。対象外: 「読んだ」「確認した」レベルの 1 文応答、質問返答、casual chat。
### 構造

- **P**oint (結論): 一行で何が起きたか / 何を変えたか
- **R**eason (理由): なぜそれが必要だったか (incident / 不便 / 目標)
- **E**xample (具体例): how の詳細、数値、動作、file path
- **P**oint (再確認): 締めの一行、効果や次の手

10 行以上の報告は各 P/R/E/P を **太字 label** で明示する (暗黙構造は読み手に伝わらない)。10 行未満の応答は label を付けず、地の文で結論 → 理由 → 具体の順を守る。短い応答に label を機械的に繰り返すと、それ自体が機械の書いた見た目になる。

### 5W1H チェック

| 観点 | 充足例 | 欠落時の症状 |
|---|---|---|
| What | 「`/review` を 2 段階に変更」 | 「強化した」だけで何かわからない |
| **Why** (必須) | 「subagent 任せで取りこぼし発生」 | 「読み手が判断不能」「で?」と返される |
| **How** (必須) | 「parent が 7 観点で再 check、全 mode 適用」 | 抽象論で再現性なし |
| When/Where | commit hash / 日付 / file path | 通常 hash で足りる |
| Who | (省略可、necessary 時のみ) | 通常 commit author で足りる |

### 禁止 / 推奨

- **抽象語禁止**: 軸 / 層 / 経路 / 土台 / 狙い / 方針 → 具体動作 / 数値 / file path に置換
- **1 段落 ≤ 4 行**: 超える場合は箇条書きに分解
- **接続文削減**: 「背景は〜」「〜の延長線上にある」など本題と関係ない補足は削る
- **文末統一**: 体言止め混在を避け、外向き文書は「〜する / 〜した」で統一
- **同一文末の3連続回避**: 「〜した。〜した。〜した。」のように同じ語尾が3文以上続くと単調になる。常体は保ちつつ「〜した / 〜できた / 〜している / 〜となった / 〜する方針をとった」など語尾を散らす

### 例 (commit まとめ)

```text
**結論**: `/review` を 2 段階 self-check に変えた。
**理由**: 1 段階だと subagent 任せで見落としや過剰指摘が残る。
**例**: parent が 7 観点で再 check。全 mode 適用。file: `commands/review.md`
**結論再確認**: subagent 単独判定の取りこぼしを parent が拾える状態。
```

10 行未満の応答は label を付けず、同じ順序を地の文で書く。

```text
`/review` を 2 段階 self-check に変えた。1 段階だと subagent 任せで見落としが残るためで、parent が 7 観点で再 check する。file: `commands/review.md`
```

### decision 要求時の Point 変種 (decision-frame-first)

応答が user に decision を求める場合、Point は調査結果でなく **決定の枠** を提示する。trigger は (1) 末尾が `?` で締める質問 (2) 本文に選択肢 (A/B、案 1/2、Yes/No、どちらにする、どう進める 等) を含む — 両方満たす応答全て、長さ閾値なし (短文 3 行でも適用)。

冒頭 1 行に `要決定: <選択肢の枠> / <候補数>` を置く。

```text
要決定: 子 project memory が parent を継承するか (Yes/No → 子削除可否)
情報: W2 完了 (16 file 削除)、統合方針は 1 通り、最終確認 1 点

要決定: 2 widget × 各 2 案 = 4 通り (A/B 独立、A は 1 つ選ぶ / B は 1 つ選ぶ)
情報: 両 widget とも attribute 不在で 0 件確定
```

**Why**: 真 `どういうこと?` 3 件 (2026-05-28 / 05-30) の root cause は「冒頭結論が `調査結果` を語り、user が返すべき内容 (decision の枠) が末尾質問に隠れていた」。長文冒頭結論 rule は trigger 満たしても「結論=調査結果」を許す書き方で素通り。短文ケース (3 行) は長文 rule の trigger 外で素通り。詳細: `~/.claude/references-private/analysis-doukouiukoto-pair-20260603.html` (private 保管、social hit 含有のため public repo 外置き)

## chatとdocumentで文体を分ける

| 場面 | 方針 |
|------|------|
| Chat応答 | 開いた文章 (`rules/plain-jp.md`)、結論先出し、短く速く |
| ヒト向けdocument | 文として完結 (〜する / 〜した)、主語明示、地の文と箇条書き混在、常体統一 |

**document基本ルール**:

- 主語必須 (「担当者は〜」「ユーザーは〜」)
- 指示語禁止 (「これ」「それ」「上記」「前述」→ 具体名)
- 略語禁止 (初出はフルスペル)
- 「適宜」「状況に応じて」禁止 → IF-THEN形式で明文化
- 日付YYYY-MM-DD、英数字半角

### chat 草稿 → 外向き翻訳の 8 観点 sweep

chat の体言止め圧縮をそのまま外向き (PR / Slack / Notion / 報告) に流用しない。
下書き完成後、以下 8 観点で sweep してから提示する。

1. **体言止め排除 (常体統一)**
   - NG: 「3.5-7 分で完了見込み」「余裕大」「全観点クリア」「追加リスクなし」
   - OK: 「3.5〜7 分で完了する見込みになる」「十分な余裕がある」「いずれの観点でも問題は発生しない」「追加のリスクはないと判断する」
2. **略記を具体数値・正式名称化** — 詳細は AI 臭 3 変換 (a) 参照
3. **難解漢語・jargon 排除** — 「外挿」「諸元」等 → 平易表現。あわせて **漢字 5 字以上の連続を作らない** (例: 「段階的強化」→「段階的に強化」、「逐次化検出」→「逐次に並ぶ状態を検出」、「絶対禁止徹底」→「禁止を徹底する」)。固有名詞・技術用語は対象外、漢語連結のみ対象。機械検出は `grep -oE '[一-龯]{5,}'`
4. **指示語・矢印・記号 → 文展開**
   - NG: 「上記」「これ」「それ」「→ 本番でも数秒〜10 秒」
   - OK: 「前項の設定値」「<具体名>」「〜のため、本番でも数秒から 10 秒程度で完了する」
5. **主語明示** — 「実走した」→「<実施者> が実走した」または「テストとして実走した」
6. **表のセル内も単位明示**
   - NG: `188K×1` / `9秒` / `mismatch=0`
   - OK: `188,000 行 × 1 件` / `9 秒` / `mismatch=0 件`
7. **曖昧名詞は範囲明示**
   - NG: 「テスト完了」「動作確認」
   - OK: 「inventory_batch と notification_service を本番想定手順で実行した」
8. **AI 定型語 / カタカナ造語の事前 sweep** — NG 辞書から grep

詳細運用は「[## 機械検出 grep (出力前 sweep)](#機械検出-grep-出力前-sweep)」section と連動する。

### UI コピー / エラーメッセージ向け追加規約

ユーザー向け UI 表示文言・エラーメッセージは「やさしい日本語」基準を適用する。

- **1 文 40 字以内** (技術文書 100 字 / web 60 字とは別の最厳格 layer)
- 難読漢語を避け、小学校 4 年生程度の語彙を選ぶ
- 専門用語の代わりに具体動作を書く (NG: `認証エラー` / OK: `ログインに失敗しました。パスワードを確認してください。`)
- 一文一意 (複文 / 並列禁止)

出典: 総務省「やさしい日本語ガイドライン」。在留外国人 / 高齢者 / 児童含むユーザーが対象の場合は本規約を最優先。

## 構造原則 (docs 観点)

- **散文より bullet ファースト**: 説明は bullet で書く、地の文は接続・背景・結論のみ
- 各セクション直下に **目的 1 行** を添える
- 強調太字は GO/NO-GO 基準等の **限定箇所のみ** (濫用で埋没)
- forward reference は **anchor link 化** (「○○参照」より `[○○](#xxx)`)
- 用語は **他参照で再定義しない** (用語ポインタ表に統一)
- 環境・テストデータ等は **別 SoT 参照**、本書では再記しない

## 箇条書き階層化

- 因果連鎖・所属関係・補足説明は **親子で表現** (平坦に並列禁止)、2 スペースインデント統一
- 同一階層に並列するのは **対等な並列のみ**

## 箇条書きスタイル (結論 → 理由 → 補足の 3 段)

外向き doc (DesignDoc / PRD / レビュー結果) 生成時の bullet 記述ルール。

- **結論を先に書く** (行頭 = 結論、根拠は下段)
- **下に理由をぶら下げる** (複数行可、2 スペースインデント)
- **理由の下に補足を 1 段だけ** ぶら下げてよい (結論 → 理由 → 補足の 3 段構造)
- **bullet 内も文として完結させ、句点で閉じる** (`rules/plain-jp.md` と同一規範)。体言止めを許すのは表のセルと見出しだけ
- **1 行で読めるなら分けない**
- 階層は 3 段まで。それ以上深くしない
- 3 段で収まらない複雑な内容は段落文に切り替える

## ミクロ規則

- sentence-length **120 字超は分割**
- 助詞重複 (「のの」「をを」「がが」) 排除
- 絶対表現 (「必ず」「絶対に」「完全に」) は GO/NO-GO 基準での使用を推奨し、説明文では「〜するとよい」「〜が望ましい」など穏やかな表現に言い換えるとよい
- セクション記号 `§` は使わない (日本語では「Phase 2」直書きで足りる)
- 「埋める」より「記録する」「測定値を記入する」 (docs 寄り)
- web 出力時は `## Web 可読性` の 60 字上書き適用

## Web 可読性 (scan 前提)

外向き prose は web (GitHub / GitLab / Notion / Slack / Confluence) で読まれる。読者は読まずに **scan する** (NNG: 79% scan / 16% line-by-line)。chat 応答と別の構造が要る。

### scan pattern 対応

| pattern | 読者行動 | 書き手の対応 |
|---|---|---|
| F-pattern | 上から左寄り、段落先頭数語のみ拾う | 段落先頭に keyword、左端に情報 |
| layer-cake | 見出しだけ拾って必要箇所だけ本文へ | 見出しを「主張」化、descriptive に |
| spotted | 太字 / link / 数値だけ拾う | keyword を太字、評価語に数値併記 |

外向き文書生成時 (`/git-push --pr` / `/post-comment` / `/docs`) は 3 pattern の **どれで読まれても主旨が伝わるか** を self-check する。

### Web 用 ミクロ規則

- **1 文 60 字以内** (最大 80 字)、読点 3 個まで — 「## ミクロ規則」120 字を web 文脈では上書き
- **1 段落 3-4 行 / 250 字以内** — 段落間に空行必須
- **漢字比率 3 割目安** — 「行う / 出来る / 事 / 物 / 為」→「やる / できる / こと / もの / ため」
- **見出しは主張化** — 「アーキテクチャ」 NG / 「読み書き分離で負荷分散」 OK
- **結論先出し + 本文量半減** (inverted pyramid)
- **数値 / file path は太字 or `code`** — spotted pattern で拾われる確率上昇
- **見出し / キャッチは意味単位で改行** — Notion / Slack は実 viewer で preview
- **コードブロック / list の前後に空行** — renderer 差で潰れる対策
- **heading 前 2 語に keyword (front-loading)** — F-pattern で末尾は読まれない。先頭語が情報の核になるよう語順反転 (NG `データ可視化による不正検出の早期化` / OK `不正を 24% 早期検出: データ可視化導入`)
- **heading の文脈外自己完結** — search result / SNS feed / RSS で heading のみ露出するため、具体的事実 (数値 / 対象) を含める
- **heading の読点 3 個以下** — heading は 1 メッセージに絞る、句読点最小化
- **a11y: 色 + 書体 / 文言の二重識別** — 強調はボールド + 色、必須項目は「赤字」でなく「赤い ※ のついた」、色弱者 / screen reader 利用者に届く
- **WCAG 3.1.5 reading level**: 中学卒業相当 (連続ひらがな + 漢字長の計算式) を超えるなら平易版 or 要約ページを併記
- **モバイル: 難解語は読速を落とす** — comprehension は維持されるが読速低下で離脱する。SP 想定なら語数削減 + 平易語選択

### 出力前 web 用 追加 check (既存 7 項目に追加)

- [ ] 1 文 60 字以内 / 読点 3 個以内
- [ ] 見出しが主張型 (ラベル型でない)
- [ ] 段落 3-4 行以内、空行で区切り
- [ ] keyword / 数値 / file path が太字 or `code` で scan 可能

## 逆に考えるミニマル化

「何を削るか」より「**essence で何を残すべきか**」から発想する。

- 別 SoT で代替できる箇所は積極的に削除し、リンク参照に切替
- 環境構築 / 撤去 / 用語定義は外部 SoT に寄せる
- 本文書固有の価値 (GO/NO-GO 閾値 / 結果記録欄 / 当日手順) のみ残す

## 文単位の品質規約

1 文単位の品質 (文長 / 読点 / 連続漢字 / ひらく漢字 7 品詞 / 冗長圧縮 / 弱い表現禁止 / 逆接「が」/ `!?` 不使用 / 漢数字 / 見出し常体 / 全角かっこ) は textlint preset (ja-technical-writing / JTF-style) の細則層のため `references/writing-sentence-rules.md` へ分離した。深い書き直し / `/jp-writing` 実行時に参照する。機械 sweep は `scripts/jp-textlint.sh <file>` (連続漢字≥5 / 読点≥4 / 文長>100 / NG 辞書 hit) で一括検出できる。

## AI臭の根本: 書き手不在

AI 臭の正体は表層の記号や常套句でなく **書き手の不在** にある。主語が誰でもなく、見出しに評価が滲み、段落の呼吸が一定で、読み手に渡したい結論が不明。記号を直しても書き手が居なければ AI 臭は残る。下記 3 軸で「自分が書いた痕跡」を本文に残す。語彙 NG (L3) と約物 (L5) は `NG-DICTIONARY.md` / `pre-tool-use.sh` が機械検出するため本 section では扱わない。

### (1) 主体を明示する (False agency 除去)

主体不明の総称断定 (「多くの〜」「一般に〜」「よく〜される」) は書き手不在の最頻形。主語を **特定の人 / PR / doc / system** に置き換え、根拠を 1 文添える。

| NG | OK |
|----|----|
| 多くのエンジニアが型定義に苦労する | 私はこの PR で型定義を 3 度書き直した。原因は generic 越しの推論が ts 5.4 で変わったため |
| 一般に N+1 は避けるべきだ | 本 service は list API で N+1 を許容している。1 list 50 件 × 平均 200ms の余裕があり、preload 実装の保守コストを上回らない |
| よく挙げられるのは可読性の向上だ | 本 refactor の目的は 1 PR で触る file 数を 5 以下に抑えることだ。週次 review 時間が 90 分 → 45 分に減る |

### (2) 見出しを中立名詞句に降ろす

PRINCIPLES.md `## scan pattern 対応` で「見出しの主張化」を推奨するが、**主観形容詞 (重要 / 効率的 / 強力 / シンプル) が混じる主張化** は AI 臭の温床。事実 / 数値 / 対象を含む中立名詞句に降ろす。形容詞を入れるなら根拠を添える。

変換の具体例 (効率的な → 数値、強力な → 件数) は下の `## AI臭を消す3変換 → (a) 抽象語 → 数字 / 事例` の table と共通する。見出しに使う場合も同じ「抽象語 → 数字」変換を適用する。

### (3) リズムを意図的に揺らす

AI 出力は段落長 / 文長 / 文末が均質化する。意図して揺らすと書き手の呼吸が出る。`Web 可読性` の「1 段落 3-4 行」は上限規則であり、**全段落を 3-4 行に揃える指示ではない**。

- 1 段落 1 文の段落を混ぜる (結論段 / 注意喚起段)
- 短文 (10-30 字) と中文 (60-80 字) を交互に置く
- 文末「〜する / 〜した / 〜だ / 〜ない」を 3 連続で同じにしない
- 体言止めは全 context で使わない (`rules/plain-jp.md` 適用範囲)

機械検出は困難 (構造の意図性は読まないと分からない)。`/jp-writing` 実行時にこの観点を self-check する。

## AI臭を消す3変換

### (a) 抽象語 → 数字 / 事例

| NG | OK |
|----|----|
| レスポンス速度を大幅改善 | p99 1.2s → 320ms (-73%)。Redis cache + N+1削除 |
| エラー処理を強化 | 5xx日次120件 → 8件。`context.WithTimeout` を全handlerに追加 |
| 効率的に作成可能 | md直接投稿できる。手動コピペ廃止が目的 |

### (b) 評価語 → 根拠1文併記

「適切な / 最適な / 改善 / 重要 / 必須 / 推奨 / 一般的に / 強化 / 向上 / 活用」を書くなら**直後に1文で根拠** (なぜ・事例・数字)。

| NG | OK |
|----|----|
| セッショントークンの適切な管理が必須 | session tokenはcookie `httpOnly` に。localStorageはXSS経由で盗まれ、tokenにexpiryしかないため全アカウント乗っ取り可 |

### (c) 難語 → 削除でなく定義併記

初出で1文の定義を併記。専門用語を削ると正確さを失う。

| NG | OK |
|----|----|
| idempotencyを担保 | idempotency (同リクエストを2回叩いても結果不変) を担保。retryで二重注文発生しない |
| CQRSで整合性 | CQRS (読み書きを別モデル分離) で整合性。readはreplicaから返せてscale可 |

### (d) 英単語 → 日本語の平易表現

詳細置換表: `PRINCIPLES-word-replace.md` 参照。

### NG辞書

hook が動的抽出する NG 語 list は **[NG-DICTIONARY.md](NG-DICTIONARY.md)** に集約。key 名 / 記法は 1 文字も変えない。

**内部jargon初出和訳必須**: taigen-dome (体言止め) / canonical (基準) / inject (注入) / scope (範囲) / fallback (代替動作) / boundary (境界)

**略語初出展開必須**: DoD (Definition of Done) / RCA (Root Cause Analysis) / PRD (Product Requirements Document) / DD (Design Doc) / SPOF (Single Point of Failure) / ROI (Return on Investment) / MVP (Minimum Viable Product) / WTP (Willingness to Pay) / MECE (Mutually Exclusive, Collectively Exhaustive)

**硬い文語 (柔らかく)**: 〜である / 〜であった / 〜されている / 〜となる / 〜に基づき / 〜に関して / 当該 / 以下に示す

## 避けるパターン (段落・文章全体の構造)

| パターン | 症状 | 対処 |
|----------|------|------|
| 抽象的なポエム | 名詞だけで構成、絵が浮かばない | 具体的な状況・行動・数値に置換 |
| 用語の羅列 | 専門用語が並ぶが各々の関係が不明 | 関係を1文で繋ぐか、1つに絞る |
| 過剰に丁寧な表現 | 「いただきますよう〜」「〜していただければ〜」 | 直接的な動詞文に短縮 |
| 余分な形容詞 | 「強力な」「革新的な」「柔軟な」が情報を持たない | 削除or数値 / 事例に置換 |
| 同義反復 | 同じ内容を言い換えて繰り返す | 1箇所に集約 |
| 中身のないまとめ | 「以上をまとめると重要だ」で何も結論してない | 1文の結論を書くか、まとめ自体を削除 |
| PR 文脈依存の表現 | 「DD §X-Y 参照」「incident (YYYY-MM-DD) で観測」等、半年〜1年後の読者に通じない社内文書参照・日付 | 必要情報は本文に書き起こし、社内URL・過去日付は削除する |
| 時限的マーカー (merge 後意味喪失) | 「本 PR で新設」「本 commit で削除」「先週合意した通り」「上述の通り」「直近の incident で」等、merge / 投稿後の読者に通じない時限文言 | 削除 or timeless 表現に置換 (「実装初期」「初版」等は OK)、状態完了マーカー (「対策反映済み」) は timeless として残す |
| 同 file 内 section 重複 (議論ポイント表 / 監視 / 採用理由 等) | 同論点が複数 section に分散、片方が要約 / 片方が詳細でも内容重複 | 詳細 section を残し、要約側は anchor link 1 行に圧縮。即統合できない場合は併存維持で別 task 化 |
| 複合名詞スタック | タイトル / 見出しで複合名詞を 3 個以上連結し、動詞・助詞が抜ける | 複合名詞は 2 個まで。3 個以上は動詞文に変換する |
| 自己物語導入 | 「本日のセッションで〜」「あわせて〜」等、書き手の作業経緯から書き始める | 読み手の判断に必要な事実から書き始める |

## 出力前セルフチェック (7項目、6項目以上で合格)

```
[ ] TL;DR / 結論が冒頭 1-3 文 (「本稿では〜」削除済)
[ ] 「必須」「推奨」「重要」に 1 文の根拠併記
[ ] 初出専門用語に定義併記
[ ] 抽象語 (改善 / 最適化 / 効率化) の代わりに数字 or 事例
[ ] 読み手の次アクション (approve / 実行 / 質問) が末尾明示
[ ] `## 書く前の4問` (L27) の 4 問に答えてる
[ ] 経過メモゼロ (「当初は」「以前は」「〜に変更した」「レビュー指摘で」を grep、現在の事実のみ)
```

未達は修正してから出力。投稿 / commit / push前にAI自身が確認。
web 出力時は `## Web 可読性` の追加 check 4 項目も適用。

## 機械検出 grep (出力前 sweep)

self-check 7 項目通過後、残存 NG の機械検出を 1 行 grep で実施する。
chat draft 段階では hook block が発火しないため、自走 sweep を規範とする。

**一括検査**: `scripts/jp-textlint.sh <file>` で 連続漢字≥5 / 読点≥4 / 文長>100 / NG辞書 hit をまとめて検出できる (textlint 相当、手動起動)。pre-tool-use hook は外向き書込時に連続漢字・読点を warn-only で自動検出する (block はしない)。

### 基本 grep (略記・記号・jargon)

```bash
grep -nE '(M 行|K 行|→|外挿|諸元|逐次伝播|long TX|replica lag|disk I/O)' <draft>
```

### NG 辞書由来 grep (AI 定型語 / カタカナ造語禁止)

NG 語 canonical: [NG-DICTIONARY.md](NG-DICTIONARY.md)。全 key (AI定型語 / カタカナ造語禁止 / 難読漢語 / 弱い表現 / 冗長表現 / 非日常英語) の literal はそちら参照。hit したら該当行を書き換えてから出力する。

## 全体マップ — 各ルールがどう認知負荷を下げるか

ルールは「読み手が払うはずだった負荷を書き手が肩代わりする」ためにある。形だけのルール準拠はAI調に戻る。

| ルール | 認知負荷をどう下げるか |
|--------|---------------------|
| 4問 | 「誰のためか」を最初に絞り、読み手が自分事として読める |
| 媒体別構造 | 形が決まっていれば読み手は内容に集中できる |
| 文体分離 | documentは常体・主語必須・指示語禁止 → 読み返し不要 |
| 3変換 | 抽象語の解釈労力を書き手が引き受ける |
| 守る指針 | 段落ごとに役割を持たせ、見出しだけで流れが伝わる |
| セルフチェック | 文体崩れを機械的に検出、見落としを救う |

迷ったら「各ルールは読み手の負荷をどう下げるか」を自問する。答えられないルール適用は形だけになる。

## 関連

- [初読者基準](#初読者基準--初めて読む人でも分かる文章) — 読者想定・語彙基準・初出定義の統合節 (本 file 内)
- [commit-message.md](commit-message.md) — コミットメッセージ固有
- [pr-description.md](pr-description.md) — PR本文 + レビュー応答
- [external-post.md](external-post.md) — 短文向け詳細 (PREP / ストーリー型 / 5軸採点)
- [long-form-doc.md](long-form-doc.md) — 長文向け詳細 (TL;DR / 3層リライト / ADR/PRD/EARSテンプレ)
- [design-doc-protocol.md](design-doc-protocol.md) — DesignDoc専用プロトコル
- [auto-knowledge-update.md](auto-knowledge-update.md) — セッション中の学び追記
- `references/writing-patterns.md` — 詳細パターン集 (textlint / 図表型 / 書き直しPhase / 専門用語置換)
- `references/on-demand-rules/ai-output.md` — 強制ルール (PR/commit/code-comment禁止事項)

## アンチパターン (統合済)

旧 writing_failure_* memory は本 file と guideline に統合済み。複合名詞スタックは「避けるパターン」表、link 過多は [external-post.md](external-post.md) の「link 過多を避ける」、報告型 doc の冒頭結論 (最大値 / 測定条件 / 次アクション) は「媒体別構造 > 長文」を参照する。新規失敗を観測したら `/retrospective` Phase 3.5 で memory を追加する。
