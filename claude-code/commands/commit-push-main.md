---
allowed-tools: Bash, Read, Grep, Glob
description: 統合済み - /git-push --main を使用してください
---

> **統合済み**: このコマンドは `/git-push --main` に統合されました。
> `/git-push --main` を実行してください。引数はそのまま渡します。

`/git-push --main` $ARGUMENTS を実行。

ARGUMENTS: $ARGUMENTS
