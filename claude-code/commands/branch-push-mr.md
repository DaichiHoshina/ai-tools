---
allowed-tools: Bash, Read, Grep, Glob
description: mainからブランチ切り → commit → push → MR/PR作成を1コマンドで実行
---

> **統合済み**: このコマンドは `/git-push --branch` に統合されました。
> `/git-push --branch` を実行してください。引数はそのまま渡します。

`/git-push --branch` $ARGUMENTS を実行。

ARGUMENTS: $ARGUMENTS
