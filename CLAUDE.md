# CLAUDE.md (pointer)

本体は `CLAUDE.repo.md` にある。`claudeMdExcludes` 設定 (`claude-code/templates/settings.json.template`) が repo 直下の CLAUDE.md を読み込み除外するため、実際の rule は owner 階層 CLAUDE.md からの import で `CLAUDE.repo.md` を読み込む構成にしている。本 file は GitHub 上の案内用で、Claude Code には読み込まれない。
